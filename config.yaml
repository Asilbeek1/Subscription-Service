env: development #local development production
postgres:
  host: localhost
  port: 5432
  sslmode: disable
  max_open_conns: 25
  conn_max_lifetime: 5m
http_server:
  port: 8080
  timeout: 4s
  idle_timeout: 60s

